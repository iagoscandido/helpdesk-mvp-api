spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/helpdesk_db'
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

    jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      hibernate:
        ddl-auto: update
      show-sql: true

# adminer is already using port 8000
server:
  port: 8001


    
